export const LAUNCH_MODE="{LAUNCH_MODE}";
export const API_CONFIG = {
    game: "{config:property('frontend.api.game') ?: '/api/game'}",
    run: "{config:property('frontend.api.run') ?: '/api/run'}"
};